<div id="chapter-menu-wrapper"><slot /></div>

<style lang="scss">

    #chapter-menu-wrapper {

        :global( h3 ) {
            color: var(--yellow-heading-color);
            font-family: "Harmonia Sans";
            font-size: 28px;
            margin-bottom: 16px;

            :global( #topic-index ) { color: var(--red-heading-color) }
        }

        :global( a ) {
            color: var(--heading-color);
            font-family: "Akkurat";
            font-size: 22px;
        }


        :global( ol ) { list-style: none }

        :global( ol > .topic ) { margin-bottom: 32px }

        :global( ol ol ) {
            display: grid;
            grid-template-columns: min-content auto;
            align-items: baseline;
        }

        :global( ol ol > #chapter-index ) {
            grid-column: 1;

            color: var(--heading-color);
            font-family: "Akkurat Mono";
            font-size: 20px;
            padding-right: 15px;
        }

        :global( ol ol > .chapter ) { grid-column: 2 }
    }

</style>

<div id="chapter-menu-wrapper"><slot /></div>

<style lang="scss" is:global>

    #chapter-menu-wrapper {

        h3 {
            color: var(--yellow-heading-color);
            font-family: "Harmonia Sans";
            font-size: 28px;
            margin-bottom: 16px;

            #topic-label { color: var(--red-heading-color) }
        }

        ol { list-style: none }

        ol ol {
            display: grid;
            grid-template-columns: max-content auto;
            align-items: baseline;
        }

        .topic { margin-bottom: 32px }
        .chapter { display: contents }

        #chapter-index {
            grid-column: 1;

            color: var(--heading-color);
            font-family: "Akkurat Mono";
            font-size: 20px;
            padding-right: 15px;
        }

        #chapter-title {
            grid-column: 2;

            color: var(--heading-color);
            font-family: "Akkurat";
            font-size: 22px;
        }
    }

</style>

<div id="side-menu-wrapper">
    <slot />
</div>

<style lang="scss">

    #side-menu-wrapper {
        
        :global( h3 ) {
            color: var(--heading-color);
            font-family: "Harmonia Sans";
            font-size: 18px;
            margin-bottom: 3px;
        }

        :global( a ) {
            color: var(--heading-color);
            font-family: "Akkurat";
            font-size: 17px;
            padding-left: 12px;
        }
        
        :global( a:hover ) { color: var(--yellow-heading-color) }

        :global( ol ) { counter-reset: topic }

        :global( ol > .topic ) {
            counter-increment: topic;
            list-style: none;

            margin-bottom: 15px;
        }

        :global( ol ol ) {
            counter-reset: chapter;
            list-style: none;

            display: grid;
            grid-template-columns: min-content auto;
        }

        :global( ol ol > .chapter ) {
            counter-increment: chapter;

            display: grid;
            grid-column: 1/-1;
            grid-template-columns: subgrid;

            align-items: baseline;
        }

        :global( ol ol > .chapter::before ) {
            content: counter(topic) "." counter(chapter);
            color: var(--heading-color);
            font-family: "Akkurat Mono";
            font-size: 16px;
        }
    }

</style>

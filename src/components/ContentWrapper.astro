<article id="content-wrapper"><slot/></article>

<style lang="scss">

    #content-wrapper > {

        :global( h1, h2, h3 ) {
            color: var(--heading-color);
            font-family: "Harmonia Sans";
        }
        
        :global( h1 ) {
            font-size: 56px;
            margin-top: -10px;
            margin-bottom: 42px;
        }

        :global( h2 ) {
            font-size: 34px;
            margin-top: 48px;
            margin-bottom: 18px;
        }

        :global( h1 + h2 ) { margin-top: 42px }

        :global( h3 ) {
            font-size: 26px;
            margin-top: 32px;
            margin-bottom: 18px; 
        }

        :global( p ) {
            color: var(--text-color);
            font-family: "Akkurat";
            font-size: 18px;
            margin-top: 1em;

            a { color: var(--highlight-color) }
        }
    }

</style>